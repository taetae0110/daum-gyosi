# 다음교시 — v1 스펙

> 워킹타이틀 "다음교시" (가칭, 나중에 바꿔도 됨).
> 이 문서는 슈퍼앱 로드맵의 **1단계**다. v1의 목표는 하나: **학생이 시험기간이 아니어도 매일 여는 시간표 앱.**
> 좁게 완벽하게. 여기 없는 기능은 만들지 않는다.

## 한 줄 정의

학교·학년·반만 고르면 시간표와 급식이 자동으로 채워지고, 수업 중엔 다이나믹 아일랜드에 "지금 무슨 과목, 몇 분 남았는지"가 떠 있는 앱.

## 차별점 (왜 이게 이기나)

- 기존 지배자(컴시간알리미)는 UI가 낡았고 다이나믹 아일랜드가 없다.
- 입력이 없다: 나이스 API가 시간표·급식을 자동 공급. 설치 30초 후 가치가 나온다.
- 서버가 없다: 앱이 나이스를 직접 호출하고, 카운트다운은 iOS가 자체 갱신. 운영비 0원.

## v1 범위 — 화면 2개 + Live Activity 1개

### 화면 1: 온보딩 (첫 실행 1회)

1. **학교 검색** — 이름 입력 → 나이스 학교 검색 → 동명 학교는 "지역 + 학교종류"로 구분해 표시
2. **학년 / 반 선택** — 숫자 피커 2개
3. **교시 시간 설정** — 나이스에는 종소리 시각이 없다(교시 번호만 줌). 기본 템플릿을 제시하고 사용자가 수정:
   - 고등: 1교시 08:30 시작, 50분 수업 + 10분 쉬는시간, 점심 4교시 후
   - 중등: 45분 수업 기준 템플릿
   - 등교 시각과 수업 길이만 조정하면 나머지 자동 계산. 교시별 개별 수정도 가능.

저장: 전부 로컬(UserDefaults 수준). 계정 없음, 로그인 없음.

### 화면 2: 오늘 (메인, 유일한 화면)

- 오늘 시간표 세로 리스트. **현재 교시는 크게 하이라이트** + 남은 시간 표시
- 급식 카드 (중식 기본, 알레르기 번호는 아이콘/툴팁으로)
- 하단에 내일 시간표 미리보기 (접힌 상태)
- 당겨서 새로고침 = 나이스 재조회. 응답은 주 단위로 캐시 (오프라인에서도 동작)
- **오늘 데이터가 아직 없으면** (나이스 업로드 지연 — 실측으로 확인된 정상 상황) 캐시된 가장 최근 요일의 시간표를 "지난 시간표" 라벨로 표시

### Live Activity (다이나믹 아일랜드)

- **시작**: 앱을 열면 그날 수업 종료 시각까지 Live Activity 시작 (8시간 제한이 등교~하교에 맞음)
- **컴팩트 (아일랜드 축소)**: 과목명 + 종료까지 카운트다운
- **확장 (길게 누름)**: 현재 교시 · 다음 교시 · 급식 한 줄
- **쉬는시간**: "쉬는시간 · 다음: 수학 (10분)"으로 전환
- **구현 핵심**: `Text(timerInterval:)` 사용 → 푸시·서버 없이 시스템이 초 단위 갱신. 교시 전환 시점은 앱이 미리 계산한 스케줄로 로컬 업데이트.
- 잠금화면 Live Activity는 자동으로 따라옴 (같은 코드)

## 명시적 비범위 (v1에서 안 만드는 것)

과제/수행평가 입력 · 학부모 기능 · 커뮤니티 · 알림/푸시 · 홈 위젯 · 안드로이드 · 계정/서버 · 시간표 직접 편집(v1.1 후보) · 다크모드 커스텀 테마

"이것도 넣을까?"가 떠오르면 이 목록을 본다. 답은 v2다.

## 기술 스택

| 항목 | 선택 | 이유 |
|---|---|---|
| 언어/UI | Swift + SwiftUI | Live Activity 위젯은 어차피 SwiftUI 강제. 크로스플랫폼 우회가 오히려 비쌈 |
| Live Activity | ActivityKit + WidgetKit | iOS 16.1+, 아일랜드 UI는 14 Pro 이상 (보유한 15 Pro로 실기기 테스트) |
| 데이터 | NEIS 오픈 API 직접 호출 | 서버 불필요. 무료 키 발급: open.neis.go.kr |
| 저장 | UserDefaults + 파일 캐시 | 설정 5개 + 주간 시간표 JSON이 전부. DB 불필요 |
| 최소 지원 | iOS 16.2 | Live Activity 현행 API (`ActivityContent`) 하한 |
| 빌드 | GitHub Actions (macOS 러너) + XcodeGen | **맥 없이 개발** — 코드는 Windows에서, 컴파일은 CI에서, 설치는 TestFlight로. 절차는 README |

## NEIS API — 실측으로 확인된 사용법과 함정

> 2026-08-26, 에이전트 6개가 실제 API 64회 호출로 검증. 추측 없음. 실행 가능한 검증 코드는 [neis.py](neis.py).

### 반드시 알아야 하는 것 (앱 설계를 바꾸는 것들)

1. **API 키는 필수다.** 키 없는 호출은 **모든 응답이 조용히 5행에서 잘리고 pIndex/pSize가 무시된다** (에러 없음, `list_total_count`만 진짜 총계). 즉 키 없이는 6·7교시가 증발하고, 학교 검색은 5개까지만, 학사일정도 5건까지만 나온다. [open.neis.go.kr](https://open.neis.go.kr)에서 무료 발급 — **개발 시작 전 1순위 할 일.**
2. **시간표 업로드가 늦다.** 수요일(8/26)에 조회하니 이번 주는 월요일(8/24) 데이터만 존재 (`LOAD_DTM`이 당일). "오늘 시간표가 아직 없음"이 정상 상황 — 앱은 빈 화면 대신 **가장 최근 있는 날의 시간표 + "아직 안 올라왔어요" 상태**로 처리해야 한다. 캐시가 UX 기능인 이유.
3. **INFO-200(데이터 없음)은 4가지 의미를 겸한다**: 주말/휴일, 아직 업로드 안 됨, 잘못된 학기 필터, 잘못된 학교 코드. 구분 불가 — "학교를 찾을 수 없음" 같은 단정적 에러 문구를 쓰면 안 된다.
4. **학사일정에 시험이 실려 있다**: `EVENT_NM`에 중간고사(10/12~13)·기말고사(12/9~11)가 하루당 1행으로 존재, 학년별 Y/N 플래그 포함. **시험 D-day(v1.1)는 데이터가 이미 준비된 셈.** 시험 감지는 `EVENT_NM`에 "고사" 포함 여부로 (SBTR_DD_SC_NM으론 감지 불가).

### 엔드포인트 사용법

| 용도 | 엔드포인트 | 핵심 파라미터 |
|---|---|---|
| 학교 검색 | `/hub/schoolInfo` | `SCHUL_NM`(부분일치!) + `SCHUL_KND_SC_NM`(서버 필터 동작) |
| 시간표 | 초등 `elsTimetable` / 중 `misTimetable` / 고 `hisTimetable` — `SCHUL_KND_SC_NM`으로 분기 | 학교코드쌍 + `ALL_TI_YMD`(하루) 또는 `TI_FROM_YMD`/`TI_TO_YMD`(범위) + `GRADE` + `CLASS_NM` |
| 급식 | `/hub/mealServiceDietInfo` | 학교코드쌍 + `MLSV_YMD` (`MMEAL_SC_CODE` 1조식/2중식/3석식 필터 동작) |
| 학사일정 | `/hub/SchoolSchedule` | 학교코드쌍 + `AA_FROM_YMD`/`AA_TO_YMD` (`AY`는 조용히 무시됨) |

### 파싱·요청 규칙 (하나라도 어기면 조용히 틀린다)

- 학교의 영구 키는 **`ATPT_OFCDC_SC_CODE` + `SD_SCHUL_CODE` 쌍** — 이름이 아니라 이걸 저장. 이 쌍으로 모든 API가 조인된다.
- 학교 검색은 부분일치: "중앙중학교" 검색 → 부산중앙중학교 등 29곳. UI에서 `LCTN_SC_NM`(지역)+`ORG_RDNMA`(주소)로 구분해 보여줄 것.
- **시간표 요청에 `AY`/`SEM`을 넣지 마라.** 없어도 되고, 틀린 학기를 넣으면 INFO-200이 난다. 날짜만 보내는 게 안전.
- 응답 봉투가 두 모양: 성공은 `{엔드포인트명: [{head: [총계, RESULT]}, {row: [...]}]}`, 실패/빈결과는 **루트 키 없이** `{RESULT: {CODE}}`. HTTP는 항상 200이라 상태코드는 무의미. `ERROR-300` 필수값 누락, `ERROR-310` 잘못된 엔드포인트.
- **모든 값이 문자열이다** (`PERIO`, `GRADE`도). 교시 정렬은 숫자 변환 후에. `len(row) < list_total_count`면 키 없는 잘림 발생 중이라는 뜻.
- 급식 `DDISH_NM`: `<br/>`로 분리 후 trim, 알레르기는 정규식 `\(([0-9.]+)\)`로만 추출 (메뉴명 자체 괄호 "유부초밥(덕)"을 오인하면 안 됨). 알레르기 코드는 표준 1~19 표. 급식 행 정렬은 끼니 우선이라 날짜 그룹핑은 클라이언트에서.
- 초등(els) 행에는 `DGHT_CRSE_SC_NM` 키 자체가 없음 — 공유 모델에서 옵셔널로.
- 학사일정 학년 플래그는 Y/N 외에 `'*'`(해당 학년 없음)이 있다.
- 속도 제한은 분당 15회 수준에서 관측 안 됨. (Windows에서 curl 테스트 시 한글이 CP949로 깨져 INFO-200이 나는 함정이 있는데, 앱(URLComponents)과 Python(urllib)은 무관.)

## 성공 기준 (이게 되면 다음 단계)

1. 본인이 2주간 매일 연다 (시험기간 아니어도)
2. 같은 반/학교 10명이 설치하고 1주 뒤에도 절반이 쓴다
3. "이것도 돼요?"라는 질문이 나온다 → 그 질문들이 v2 백로그

## 로드맵에서의 위치

시간표(v1, 지금) → 홈 위젯 + 시험 D-day(v1.1) → 과제·수행평가 크라우드소싱(v2) → 학부모 알림 = 수익화(v3) → 그 다음이 슈퍼앱. **각 단계는 이전 단계의 성공 기준을 넘어야 시작한다.**

## 지금 할 일

1. ~~나이스 API 키 발급~~ — **완료.** 키 모드 재검증까지 통과 (6교시 확인, 분반 행 없음, 검색 페이징 정상)
2. ~~맥 마련~~ — **불필요로 결정.** GitHub Actions의 macOS 러너가 컴파일을 대신한다 (README의 밀스톤 1). 한계: 빌드 루프 5~10분, UI 픽셀 다듬기 단계에서만 맥을 잠깐 빌리는 걸 고려
3. **GitHub public 저장소 만들어 push** → Actions 초록불 = 컴파일 성공 (비용 0원)
4. **애플 개발자 계정** (연 $99) — 폰 설치(TestFlight)와 출시에 필요한 유일한 지출. 절차는 README 밀스톤 2
